# Amazon Bedrock NVIDIA Nemotron POC
streamlit>=1.36.0
boto3>=1.34.0
numpy>=1.26.0

# Guardrails (primary engine). If these are unavailable at runtime, the app
# falls back to the equivalent rails implemented in guardrails.py.
nemoguardrails>=0.9.0
langchain-aws>=0.1.0
langchain>=0.2.0
langchain-community>=0.2.0
